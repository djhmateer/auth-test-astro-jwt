---
import Layout from '../../layouts/Layout.astro';

// TypeScript guard: Prevent compiler warnings about undefined session
if (!Astro.session) throw new Error('Session not available');

// Check authentication using session
const isAuthenticated = await Astro.session.get('authenticated');
if (!isAuthenticated) {
	console.log('Unauthorized access attempt to /projects/project2');
	return Astro.redirect('/login', 302);
}

console.log('User accessed /projects/project2 page');
---

<Layout>
	<main>
		<h1>Project 2</h1>
		<p>This is a sample protected project page demonstrating session-based authentication.</p>

		<div class="content">
			<h2>Project Details</h2>
			<ul>
				<li><strong>Project Name:</strong> Sample Project 2</li>
				<li><strong>Status:</strong> In Development</li>
				<li><strong>Technology:</strong> Astro + Session Auth</li>
				<li><strong>Created:</strong> 2025</li>
			</ul>

			<h2>Features</h2>
			<div class="features">
				<div class="feature">
					<h3>🔒 Authentication</h3>
					<p>Protected by server-side session authentication</p>
				</div>
				<div class="feature">
					<h3>⏱️ Session Tracking</h3>
					<p>Monitors user access and session expiration</p>
				</div>
				<div class="feature">
					<h3>⚡ Fast Loading</h3>
					<p>Server-side rendered with Astro for optimal performance</p>
				</div>
			</div>
		</div>

		<div class="actions">
			<a href="/projects">← Back to Projects</a>
			<a href="/">🏠 Home</a>
			<form method="POST" action="/api/logout" style="display: inline;">
				<button type="submit" class="logout-btn">Logout</button>
			</form>
		</div>
	</main>
</Layout>

<style>
	main {
		padding: 2rem;
		max-width: 800px;
		margin: 0 auto;
	}

	h1 {
		color: #111827;
		margin-bottom: 1rem;
	}

	h2 {
		color: #374151;
		margin-top: 2rem;
		margin-bottom: 1rem;
	}

	h3 {
		color: #4b5563;
		margin-bottom: 0.5rem;
	}

	p {
		color: #4b5563;
		margin-bottom: 1rem;
		line-height: 1.6;
	}

	.content {
		margin: 2rem 0;
	}

	ul {
		background: #f9fafb;
		padding: 1.5rem;
		border-radius: 8px;
		border-left: 4px solid #3245ff;
	}

	li {
		color: #374151;
		margin-bottom: 0.5rem;
	}

	.features {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 1.5rem;
		margin-top: 1rem;
	}

	.feature {
		background: #f8fafc;
		padding: 1.5rem;
		border-radius: 8px;
		border: 1px solid #e5e7eb;
	}

	.actions {
		margin-top: 3rem;
		display: flex;
		gap: 1rem;
		align-items: center;
		flex-wrap: wrap;
	}

	a {
		color: #3245ff;
		text-decoration: none;
		padding: 0.5rem 1rem;
		border-radius: 6px;
		transition: background 0.2s;
	}

	a:hover {
		background: #f3f4f6;
		text-decoration: underline;
	}

	.logout-btn {
		background: #dc2626;
		color: white;
		border: none;
		padding: 0.5rem 1rem;
		border-radius: 6px;
		cursor: pointer;
		font-size: 0.9rem;
		transition: background 0.2s;
	}

	.logout-btn:hover {
		background: #b91c1c;
	}
</style>